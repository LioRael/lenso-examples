/* @generated by lenso-contract-codegen; do not edit. */
export const CAPABILITY_ID = "lenso.auth@1";
export const DESCRIPTOR_VERSION = "1.0.0";
export const PORTABLE = true;
export const CROSS_LANE_TRANSFER = false;

export type Int64 = string & { readonly __lensoInt64: unique symbol };
export type Uint64 = string & { readonly __lensoUint64: unique symbol };
export type Bytes = string & { readonly __lensoBytes: unique symbol };
export type Timestamp = string & { readonly __lensoTimestamp: unique symbol };
export type Duration = string & { readonly __lensoDuration: unique symbol };
export type OptionalValue<T> = T | null | undefined;

export interface InvocationContext {
  readonly requestId: Uint64;
  readonly deadline?: Duration;
  readonly cancelled: boolean;
  readonly callerInstance?: string;
  readonly extensions?: Record<string, unknown>;
}

export type RuntimeFailure = { readonly kind: "unavailable" | "unknown_operation" | "ambiguous_binding" | "protocol_violation" | "missing_module_factory" | "unavailable_execution_class" | "invalid_resolved_plan" | "admission_closed" | "resource_exhausted" | "deadline_exceeded" | "cancelled" | "internal" | "module_failure" | "module_restart_exhausted"; readonly detail?: unknown; readonly [key: string]: unknown };
export type UnknownDomainError = { readonly code: string; readonly payload?: unknown; readonly [key: string]: unknown };

export type StreamEvent<Message, DomainError> =
  | { readonly kind: "message"; readonly message: Message }
  | { readonly kind: "peer_half_closed" }
  | { readonly kind: "terminal"; readonly outcome: { readonly ok: true } | { readonly ok: false; readonly error: DomainError } };
export interface StreamSession<Message, DomainError> {
  send(message: Message): Promise<void>;
  receive(): Promise<StreamEvent<Message, DomainError>>;
  closeSend(): Promise<void>;
  cancel(): void;
}

export interface AuthenticateRequest {
  credential: AuthenticateRequestCredential | null;
}

export interface AuthenticateRequestCredential {
  scheme: string;
  value: string;
}

export interface AuthenticateResponse {
  assertion: AuthenticateResponseAssertion | null;
  kind: "absent" | "authenticated";
}

export interface AuthenticateResponseAssertion {
  actor_kind: string;
  assurance: string;
  audience: Array<string>;
  claims?: Record<string, unknown>;
  expires_at: Timestamp;
  issued_at: Timestamp;
  issuer: string;
  parent_provenance?: string;
  proof: string;
  subject: string;
}

export type AuthenticateError = "expired" | "invalid" | "revoked" | "unsupported" | UnknownDomainError;
export type AuthenticateInvocationError = { readonly kind: "domain"; readonly error: AuthenticateError } | { readonly kind: "runtime"; readonly error: RuntimeFailure };
export type AuthenticateResult = { readonly ok: true; readonly value: AuthenticateResponse } | { readonly ok: false; readonly error: AuthenticateInvocationError };
function isRecord(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null;
}

function validatePortableJson(value: unknown): void {
  if (typeof value === "number") {
    if (!Number.isFinite(value) || (Number.isInteger(value) && !Number.isSafeInteger(value))) throw new Error("wire JSON contains an unsafe number");
    return;
  }
  if (Array.isArray(value)) {
    for (const item of value) validatePortableJson(item);
    return;
  }
  if (isRecord(value)) {
    for (const item of Object.values(value)) validatePortableJson(item);
  }
}

export function encodeAuthenticateRequest(value: AuthenticateRequest): string { validatePortableJson(value); const wire = JSON.stringify(value); if (wire === undefined) throw new Error("request cannot be encoded"); return wire; }
export function decodeAuthenticateRequest(wire: string): AuthenticateRequest { const value: unknown = JSON.parse(wire); validatePortableJson(value); return value as AuthenticateRequest; }
export function encodeAuthenticateResponse(value: AuthenticateResponse): string { validatePortableJson(value); const wire = JSON.stringify(value); if (wire === undefined) throw new Error("response cannot be encoded"); return wire; }
export function decodeAuthenticateResponse(wire: string): AuthenticateResponse { const value: unknown = JSON.parse(wire); validatePortableJson(value); return value as AuthenticateResponse; }
export function encodeAuthenticateError(value: AuthenticateError): string { validatePortableJson(value); const wire = JSON.stringify(value); if (wire === undefined) throw new Error("Domain Error cannot be encoded"); return wire; }
export function decodeAuthenticateError(wire: string): AuthenticateError {
  const value: unknown = JSON.parse(wire);
  validatePortableJson(value);
  if (typeof value === "string") {
    if (value === "expired") return value as AuthenticateError;
    if (value === "invalid") return value as AuthenticateError;
    if (value === "revoked") return value as AuthenticateError;
    if (value === "unsupported") return value as AuthenticateError;
    return { code: value } as AuthenticateError;
  }
  if (isRecord(value) && typeof value.code === "string") return value as AuthenticateError;
  throw new Error("Domain Error must be a string or object");
}


export interface AuthClient {
  authenticate(request: AuthenticateRequest, context?: InvocationContext): Promise<AuthenticateResult>;
}

export interface AuthProvider {
  authenticate(context: InvocationContext, request: AuthenticateRequest): Promise<AuthenticateResult>;
}

export const portableValueProfile = {
  int64: "decimal-string",
  uint64: "decimal-string",
  bytes: "base64-string",
  timestamp: "RFC3339-string",
  duration: "ISO8601-string",
  missingAndNull: "distinct",
} as const;
